# Assets folder

Place additional CSV datasets here for local development. Files in this folder
and in `temp_assets/` are tracked by the password-protected integrity manifest
when the application starts.

Do not commit private or production datasets.
